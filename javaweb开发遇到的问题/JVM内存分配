一、JVM的内存分布
1、线程共享区
生命周期伴随JVM的进程的生命周期
堆:存储通过new创建的对象实例，是进行GC回收的地方
方法区：存储加载的字节码的类对象信息、常量、静态方法
  运行时常量池：字节码编译过程中产生的字面量和符号引用

直接内存
  jvm虚拟机外的内存，在NIO中，通过channel和buffer可以创建直接内存，然后通过directBuffer对象指向这块内存进行管理

2、线程非共享区
生命周期伴随着线程的生命周期，
JVM栈
存储方法执行过程中产生的局部变量，动态链接、方法出口等信息


NATIVE方法栈
本地方法执行的局部变量信息、、方法出口等信息

程序计数器
记录当前线程中cpu执行字节码位置

二、JVM中的线程
jvm线程
垃圾回收线程
中断线程

三、堆中内存分配
1、新生代1/3
Eden区
对象第一创建时分配的地方
fromSurvial区

toSurvial区


2、老年代2/3

